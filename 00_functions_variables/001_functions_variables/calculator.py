### only whole numbers = int(), float allows unwhole numbers
x = float(input("What's x? "))
y = float(input("What's y? "))

z = x / y

### Rounding Shortcut - Alternatively round(x, 2)
print(f"{z:.2f}")