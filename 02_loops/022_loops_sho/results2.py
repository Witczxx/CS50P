results = ["Mario", "Luigi", "Princess", "Yoshi", "Koopa Troopa", "Toad", "Bowser", "Donkey Kong Jr."]

results.remove("Bowser")                    # append puts Bowser into last place in list
results.insert(0, "Bowser")                 # insert puts bowser into the first place
results.reverse()                           # reverses the list order

print(results)