[project]
name = "cs50p"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Witczxx", email = "193351345+Witczxx@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
cs50p = "cs50p:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
